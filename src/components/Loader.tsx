import { Loading } from "react-daisyui";

const Loader = () => {
    return ( <div className="w-100vh h-100vh flex items-center justify-center">
        <Loading size="lg"/>
    </div> );
}
 
export default Loader;